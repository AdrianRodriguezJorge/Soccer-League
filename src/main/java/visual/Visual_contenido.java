package visual;

public class Visual_contenido {
    // El en paquete “visual” estarán todas las clases correspondientes a la interfaz de usuario.
    // Los componentes visuales deben mapear con las clases del modelo.

    //NOTA: Aclarar que desde la interfaz de usuario solamente se realizarán las llamadas de los métodos que están en los servicios,
    // no se deben realizar consultas desde estas.
}
