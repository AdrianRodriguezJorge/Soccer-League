package services;

public class Services_contenido {
    // En el paquete “services” aparecerán las clases que contendrán toda la lógica de negocio de la aplicación. Es posible hacer una clase
    // servicio por cada clase del modelo o hacer una clase servicio que englobe a varias clases del modelo. Cuando se cree una clase
    // servicio el nombre de la misma debe terminar en “Services”. Ej: xxxxServices.java. Dentro del paquete “services” debe aparecer
    // una clase llamada “ServicesLocator.java” en la que estarán todas las instancias de los servicios de la aplicación, esto permitirá
    // que desde las clases visuales solamente se cree una sola instancia a todos los servicios, precisamente a través de esta clase.
    // Por tanto en las clases visuales solamente se realizaría la instancia a la clase “ServicesLocator.java” y a través de esta se
    // accederá a todos los servicios.
}
