
package model;

public class Model_contenido {
    // En el paquete “model” aparecerán las entidades o clases persistentes las que permitirán realizar el mapeo de los atributos de 
    // las clases con los componentes de interfaz de usuario. Las clases del modelo tendrán los atributos, que se corresponden con las 
    // columnas de la tabla, con sus respectivos métodos get() y set().
}
